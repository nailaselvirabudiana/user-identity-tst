version: '3.8'

services:
  backend:
    image: noivira124/user-identity-backend:latest  # <-- GANTI yourusername!
    container_name: user-identity-backend
    restart: unless-stopped
    ports:
      - "3040:3040"
    environment:
      - NODE_ENV=production
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - JWT_SECRET=${JWT_SECRET}
      - PORT=3040
    networks:
      - app-network

  frontend:
    image: noivira124/user-identity-frontend:latest  # <-- GANTI yourusername!
    container_name: user-identity-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
